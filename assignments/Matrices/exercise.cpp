#include <iostream>
using namespace std;
/* Programa para repasar el uso de matrices en C++*/

void llenaMatriz(int matriz[][3], int ren){
    /*Recibe una matriz y la cantidad de renglones (siempre tiene 3 columnas)
    la función recibe del usuario los datos y los almacena en el arreglo*/

}


void imprimeMatriz(int matriz[][3], int ren){
    /*Recibe una matriz y cuantos renglones (siempre tiene 3 columnas)
    la función imprime a pantalla los datos de la matriz como una tabla*/
}

void sumaAMatriz(int matriz[][3], int ren, int num){
    /*Se le suma a cada elemento de la matriz el número num recibido como parámetro*/
}

int main() 
{
  //PAra la cantidad de renglones, las matrices tendrán siempre 3 columnas
    int renglones;
    cout << "Número de  renglones: ";
    //Recibe el tamaño del arreglo
    cin >> renglones;
    //Crea la matriz de renglonesX3

    //Llama a la función que llena la matriz

    //Llama a la función que la imprime

    //Llama a la función que la suma un determinado número (pídelo al usuario)

    //Imprime nuevamente la matriz con la función correspondiente

  return 0;
}
